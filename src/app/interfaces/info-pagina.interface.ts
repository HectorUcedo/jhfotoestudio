
    
export interface InfoPagina {
    // Ponemos ? para que sean opcionales
  titulo?: string;
  email?: string;
  pagina_autor?: string;
  facebook?: string;
  twitter?: string;
  instagram?: string;
  tblr?: string;
  equipo_trabajo?: any[];
}
